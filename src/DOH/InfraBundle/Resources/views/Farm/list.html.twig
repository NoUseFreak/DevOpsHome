{% extends 'DOHInfraBundle:Default:index.html.twig' %}

{% block title %}{{ parent() }} - Farm - List{% endblock %}

{% block content %}
    <div class="doh-block">
        <h2 class="doh-block-title">Farms</h2>
        <div class="doh-action">
            <ul class="list-inline">
                <li><a href="{{ path('doh_infra_farm_new') }}">New</a></li>
            </ul>
        </div>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>name</th>
                </tr>
            </thead>
            <tbody>
                {% for farm in farms%}
                    <tr>
                        <td>{{ farm.id }}</td>
                        <td><a href="{{ path('doh_infra_farm_detail', {id: farm.id}) }}">{{ farm.name }}</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
